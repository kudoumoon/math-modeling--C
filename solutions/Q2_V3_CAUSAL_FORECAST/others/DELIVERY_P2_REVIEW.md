# Q2 V3 final_v1 独立交付 P2 回执

## 范围

- 门禁：`P2_DELIVERY_DATA_EXPORT`
- 身份：独立只读审核，未参与被审源码、模型运行或导出产物编写。
- 纳入：`result2.xlsx` 全单元格读回、源码与输出哈希、5 个 annual 与 4 个 development 归档、主成本、自然时钟投影。
- 排除：LP/求解器、年度或开发期重跑、图件视觉 P2。视觉回执后续单独执行。

## 状态

**PASS（数据与导出范围）**。P0、P1 硬阻断均为 0。

Q2 总体状态仍为 `DATA_EXPORT_PASS_VISUAL_P2_PENDING`，`formal_use` 应继续保持 `false`，不得在视觉回执通过前表述为完整 P2 已完成。

## 输入快照

- 导出源码提交：`6c6a18114eee3e810ffa2f4fa9da0ae817e85254`
- 5 个年度 run 提交：`17f25a508deaf7f9248ef18c3b498be12f5c083a`
- 4 个开发 run 提交：`f5f8c1b92e56f2e63649f14d5649c56bf5144b4d`
- `q2_release_manifest.json`：`2f88f58334a422a62398732ddb28ff0a3883e01c280ae7611e00ab523e6265ab`
- `result2.xlsx`：`eb575abe88e9de5c0e579cff15e35307709abd3d86d98228a433e4da7836eee9`
- `finalize_q2.py`：`a3ec78fcb35cf2f835c5b22bd99efbc4d38be4df9689e2b9c20ef2f6be410d59`

## 证据

1. `final_v1` 恰有 40 个文件；manifest 覆盖除自身外 39 个输出，39/39 SHA-256 全部匹配。6/6 个导出源码同时匹配 live 文件与 `6c6a181` Git blob；4/4 个 executed core 快照同时匹配 live 文件与年度 run 提交。
2. 5 个 annual、4 个 development 共 9 个归档，各 10 个文件；与 `runs/` 原目录逐文件比较 90/90 字节一致。9/9 个 run manifest 的 source/input/output 哈希有效。两个批次各自提交一致，跨批次提交不同属于已冻结的开发选择与年度运行阶段差异。
3. 工作簿保留 3 张模板工作表与全部表头。购电表为 334 行、144 槽；储能表为 2004 行，即 334 日乘 6 块；紧急购电表为独立重算得到的 263 个自然时钟事件。全部数据单元读回匹配，无公式、无 Excel 错误值、无模板残留行。
4. 工作簿购电逐槽最大差 `4.55e-13 kWh`，日合计最大差 `4.37e-11 kWh`，日成本最大差 `2.18e-11 元`。储能充电、放电、SOC 最大差分别为 `5.46e-12`、`1.82e-12`、`3.64e-12 kWh`，均仅为浮点读写误差。
5. 独立成本复算为计划购电 `13,049,716.087816667 元`、紧急购电 `962,374.1199523073 元`、总成本 `14,012,090.207768975 元`；与冻结主结果总成本差为 `0`。紧急购电费用逐槽满足 5 倍价格规则。
6. 主计划窗口为 `2025-02-01 00:10` 至 `2026-01-01 00:10`；自然时钟桥为 `2025-02-01 00:00` 至 `2026-01-01 00:00`。48,096 个自然槽逐项满足：槽 0 取前一计划日槽 143，槽 1 至 143 取当日槽 0 至 142；SOC 跨槽残差为 `0`。
7. 自然时钟投影未改主计划：`q2_interval_ledger.csv` 与 `q2_policy_arrays.npz` 均和年度主 run 逐字节一致，工作簿购电量与成本逐值来自该主 ledger。自然日与计划日储能 CSV 独立重算均通过，且二者窗口确实不同。

所有核验命令均为只读并以退出码 0 完成；未调用 `finalize_q2.py`、`run_q2_v3.py`、LP 或任何模型重跑。

## 发现与返工

- P0：无。
- P1：无。
- P2：后续版本可在 release manifest 内直接登记 9 个归档树哈希，并显式列出年度/开发两个提交；本回执已补充这些证据，不阻断当前冻结版本。
- 返工：无。下一门禁仅为另行安排的 Q1/Q2 图件视觉 P2。
